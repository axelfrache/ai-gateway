services:
  ai-gateway:
    build:
      context: .
    image: ai-gateway:local
    env_file:
      - path: .env
        required: false
      - path: ../ai
        required: false
    ports:
      - "${HOST_PORT:-8080}:8080"
    environment:
      SERVER_ADDR: ":8080"
    restart: unless-stopped
